{
  "description": "Checks Claude's replies and file edits against your CLAUDE.md rules with TypeSafe Jev",
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "node \"${CLAUDE_PLUGIN_ROOT}/dist/src/cli.js\" hook stop",
            "timeout": 30
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Edit|Write|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "node \"${CLAUDE_PLUGIN_ROOT}/dist/src/cli.js\" hook post-edit",
            "timeout": 30
          }
        ]
      }
    ]
  }
}
